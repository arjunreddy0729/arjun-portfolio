# Optional. When set, the portfolio AI assistant (/api/chat) answers via Claude,
# grounded in the portfolio content. When unset, it falls back to the local
# keyword responder in lib/portfolio-knowledge.ts — the assistant still works.
#
# Get a key at https://console.anthropic.com and add it to your Vercel project
# environment variables. Never commit the real value.
ANTHROPIC_API_KEY=
